namespace JobAppTracker.Demo.Constants
{
    
    public enum JobType { Internal, External};

    public enum ApplicationStatus { NotStarted, Applied, AwaitingConfirmation, AwaitingClientInterview, Interviewd, Closed };
}
